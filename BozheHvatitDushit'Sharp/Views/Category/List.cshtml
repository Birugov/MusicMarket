@model BozheHvatitDushitSharp.ViewModels.ItemsViewModel
<h1>Instruments</h1>
<h3>@Model.currCategory</h3>
<div class="row mt-5 mb-2">
    @{
        foreach (Item item in Model.allItems)
        {
            @Html.Partial("allItems", item);
        }
    }
</div>